spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/notification?useSSL=true&requireSSL=true&serverTimezone=UTC
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}

logging:
  level:
    com.notification: INFO